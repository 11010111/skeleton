#
# Add SQL definition of database tables
#
CREATE TABLE tt_content
(
    container    VARCHAR(30) DEFAULT '' NOT NULL,
    breakpoint   VARCHAR(30) DEFAULT '' NOT NULL,
    color_picker VARCHAR(30) DEFAULT '' NOT NULL
);
